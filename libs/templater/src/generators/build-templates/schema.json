{
  "$schema": "http://json-schema.org/schema",
  "$id": "BuildTemplates",
  "title": "Inline <template x-src> includes",
  "description": "Replace <template x-src=\"file\"> placeholders with the file's content (HTML inlined as-is, JS wrapped in <script>).",
  "type": "object",
  "properties": {
    "htmlDir": {
      "type": "string",
      "description": "Directory of HTML files to process in place."
    },
    "templatesDir": {
      "type": "string",
      "description": "Directory containing the template include files."
    }
  },
  "required": ["htmlDir", "templatesDir"]
}
